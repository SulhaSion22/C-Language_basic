/*#include <stdio.h>

struct vector {
	float x;
	float y;
};

struct vector get_vector_sum(struct vector, struct vector);

int main(void) {

	struct vector a1, a2, v1;
	printf("첫 번째 벡터를 입력하세요(x y) :");
	scanf("%f %f", &a1.x, &a1.y);
	printf("두 번째 벡터를 입력하세요(x y) :");
	scanf("%f %f", &a2.x, &a2.y);

	v1 = get_vector_sum(a1, a2);
	printf("벡터의 합은 (%f, %f)입니다.", v1.x, v1.y);

	return 0;



}

struct vector get_vector_sum(struct vector v1, struct vector v2) {
	struct vector v3;
	v3.x = v1.x + v2.x;
	v3.y = v1.y + v2.y;

	return v3;

}
*/
/*#include <stdio.h>
#define STUDENT 5
int get_average(int a[], int num);

int main(void)
{
	int scores[STUDENT] = { 1,2,3,4,5 };
	int avg;

	avg = get_average(scores, STUDENT);
	printf("평균은 %d 입니다.\n", avg);
	return 0;
}

int get_average(int a[], int num)
{
	int i;
	int sum = 0;

	for (i = 0; i < num; i++)
	{
		sum += a[i];
	}
	return sum / num;
}
*/
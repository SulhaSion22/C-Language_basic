/*#include <stdio.h>

int main(void)
{
	int num;
	int i;
	int fact = 1;

	printf(" 정수를 입력하시오 : ");
	scanf_s("%d", &num);

	for (i = 1; i <= num; i++)
	{
		fact *= i;
	}
	printf(" %d !  = %d\n", num, fact);
	return 0;
}
*/

/*
#include <stdio.h>

int main(void)
{
	int num;
	int i=1;
	int fact = 1;

	printf(" 정수를 입력하시오 : ");
	scanf_s("%d", &num);

	while (i <= num)
	{
		fact *= i;
		i++;
	}

	printf(" %d ! 의 값은 %d 입니다.", num, fact);
	return 0;
}
*/
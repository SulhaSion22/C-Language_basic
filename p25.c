/*#include <stdio.h>

int main(void)
{
	int grade=0;
	int sum = 0;
	int n=0;
	double average;

	while (grade >= 0)
	{
		printf("성적을 입력하세요 : ");
		scanf_s("%d", &grade);
		if (grade < 0)
			break;
		sum += grade;
		n++;
	}

	average = (double)sum / n;
	printf("성적의 평균은 %lf입니다.", average);
	return 0;


}
*/

/*#include <stdio.h>

int main(void)
{
	int grade = 0;
	int n = 0;
	int sum = 0;
	float average=0.0;

	while (grade >= 0)
	{
		printf("성적을 입력하시오 : ");
		scanf_s("%d", &grade);

		sum += grade;
		n++;
	}
	sum = sum = grade;
	n--;

	average = sum / n;
	printf(" 성적의 평균 : %f", average);
	return 0;
}
*/